# NoSQL_PROJECT
CAMPUS EVENT MANAGEMENT & REAL-TIME NOTIFICATION SYSTEM
(Using MongoDB + Redis + Flask)

open new terminal
cd campus-events/backend
pip install -r requirements.txt
python seed_data.py      # seeds MongoDB + Redis
python app.py            # starts on http://localhost:5000
